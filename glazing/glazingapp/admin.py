from django.contrib import admin
from .models import contact, works_model, services
admin.site.register(contact)
admin.site.register(works_model)
admin.site.register(services)
# Register your models here.
